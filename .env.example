# Optional: put this file next to `supabase/` and `frontend/` (repo root).
# Vite loads both this folder and frontend/.env — frontend wins if a key exists in both.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_RAZORPAY_MOCK_SUCCESS=true
